---
phase: 04-autonomous-daily-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: true
requirements: [SCHED-01, SCHED-02]

must_haves:
  truths:
    - "ROADMAP Phase 4 describes the Coolify Scheduled Task (cron) model — not Windows Task Scheduler — and records the Windows→hosted supersession honestly rather than silently rewriting it."
    - "REQUIREMENTS SCHED-01 reads as 'runs daily automatically'; SCHED-02 is re-expressed for the hosted model (missed-run visibility via the existing staleness banner) with its Windows wake-timer wording marked superseded."
    - "README documents the exact Coolify Scheduled Task: command `python -m techtrend.daily`, a daily cron schedule, the required deploy env (PG* vars, OPENAI_API_KEY, GITHUB_TOKEN; TECHTREND_DISABLE_LLM as the enrichment opt-out), and that the scheduled task is independent of serving (D-17)."
    - "README's Configure and Deploy sections name OPENAI_API_KEY (GLM enrichment provider) as the required enrichment secret, not ANTHROPIC_API_KEY."
    - "A network-free verification proves the `python -m techtrend.daily` entrypoint chains collect→score→enrich and that health.py already renders 'last successful run' / stale messaging (SC1 & SC3 met by existing code); SC2 is documented N/A on hosted."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - README.md
  key_links:
    - "README Coolify Scheduled Task command string must match the real module path `python -m techtrend.daily` (techtrend/daily.py __main__)."
    - "Missed-run visibility is delegated to the existing techtrend/server/health.py staleness banner — no new code, no change to health.py."
---

<objective>
Rescope Phase 4 from the original Windows Task Scheduler + wake/missed-run framing to the hosted model that is actually in use: a Coolify Scheduled Task (cron) fires the existing `python -m techtrend.daily` collect→score→enrich entrypoint once a day, and a skipped day is surfaced by the existing dashboard staleness banner rather than by new catch-up code.

This is a rescope + documentation + verification phase with minimal-to-no production code, per the LOCKED decisions in 04-CONTEXT.md. No scheduler code, no APScheduler, no backfill/catch-up logic, and no change to health.py.

Purpose: Close SCHED-01 (daily automatic run) on the hosted deployment and honestly retire the Windows-specific SCHED-02 wording, keeping the planning artifacts internally consistent — mirroring how the SQLite→Postgres/Coolify reversal was recorded as decision history, not silently edited away.
Output: Rescoped ROADMAP.md Phase 4, rescoped REQUIREMENTS.md SCHED-01/SCHED-02, updated README deploy/scheduling docs, and a network-free verification that SC1 & SC3 are already satisfied by existing code + the documented Coolify config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-autonomous-daily-scheduling/04-CONTEXT.md

# The chained entrypoint the Coolify Scheduled Task invokes (already built, quick task 260816-lkt)
@techtrend/daily.py
# The existing staleness banner that satisfies the hosted missed-run-visibility criterion (do NOT modify)
@techtrend/server/health.py
# Already anticipates a Coolify Scheduled Task + /data volume; the image serves the dashboard and runs the pipeline in the same container
@Dockerfile
# Deploy + scheduling docs live here; also carries the stale ANTHROPIC_API_KEY references to correct
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescope ROADMAP.md and REQUIREMENTS.md off Windows Task Scheduler onto the hosted Coolify model</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Rescope the Phase 4 framing in ROADMAP.md and REQUIREMENTS.md from the original Windows Task Scheduler + wake-from-sleep design to the hosted Coolify Scheduled Task model, recording the supersession honestly (do not silently delete the old intent). Follow the precedent already set in .claude/CLAUDE.md for the SQLite→Postgres/Coolify reversal: keep a short note that the earlier Windows wording was superseded and why, rather than pretending it never existed.

In ROADMAP.md:
- Phase 4 one-line entry (the `- [ ] **Phase 4: Autonomous Daily Scheduling**` bullet): replace the "Windows Task Scheduler wired with wake/missed-run settings" phrasing with the Coolify Scheduled Task (cron) model — the pipeline runs unattended every day on the hosted deployment.
- Phase 4 Details **Goal**: rewrite so it describes the full collect→score→enrich pipeline running automatically once per day on the always-on hosted Coolify deployment (no cross-sleep/wake language, which is Windows-desktop-specific and N/A on a hosted server), with a missed run made visible by the existing staleness banner.
- Phase 4 Details **Success Criteria**: keep criterion 1 (runs automatically once per day, no manual action). Rewrite criterion 2 to state that the Windows wake/sleep/run-if-missed criterion is superseded — on an always-on hosted server it is N/A, and a missed run is instead surfaced by the existing "last successful run" staleness banner (no new code). Keep criterion 3 (the "last successful run" indicator reflects a same-day refresh, so a missed run is immediately visible) — noting it is already satisfied by health.py and is verified, not rebuilt, in this phase.
- Add a brief one-line supersession note under Phase 4 Details recording that Phase 4 was rescoped off Windows Task Scheduler when deployment moved to hosted Coolify (server-side PostgreSQL), consistent with the CLAUDE.md deployment reversal.

In REQUIREMENTS.md:
- SCHED-01: keep as-is in intent (the full pipeline runs automatically once per day without user action) — no wording change needed unless it references Windows; if it does, generalize to "on the deployed environment".
- SCHED-02: re-express for the hosted model. The original "survive sleep/missed-window conditions on Windows" is superseded; the hosted equivalent is missed-run visibility — a skipped day is surfaced by the existing dashboard staleness banner (HEALTH-02 behavior) rather than by wake timers. Mark the Windows wake-timer wording as superseded (strikethrough or an explicit "(superseded 2026-08-19 — see Phase 4 rescope)" note), keeping the decision history honest rather than silently editing it away.

Keep the two files internally consistent (SCHED-01/SCHED-02 in the Requirements body must match the Traceability table and the ROADMAP Phase 4 Requirements line — both stay mapped to Phase 4). This closes SCHED-01 and rescopes SCHED-02 per the LOCKED 04-CONTEXT.md decisions. Do NOT reopen the scheduler-mechanism choice or introduce any catch-up/backfill requirement.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; rm=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); rq=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); assert 'Coolify' in rm, 'ROADMAP missing Coolify rescope'; assert re.search(r'supersed', rm, re.I), 'ROADMAP missing supersession note'; assert re.search(r'supersed', rq, re.I), 'REQUIREMENTS SCHED-02 missing supersession note'; assert 'SCHED-01' in rq and 'SCHED-02' in rq; print('OK')"</automated>
  </verify>
  <done>ROADMAP.md Phase 4 (bullet, Goal, Success Criteria) describes the Coolify Scheduled Task model with an honest supersession note; REQUIREMENTS.md SCHED-01 reads as daily-automatic and SCHED-02 is re-expressed for hosted missed-run visibility with the Windows wording marked superseded; the two files remain internally consistent and both requirements stay mapped to Phase 4.</done>
</task>

<task type="auto">
  <name>Task 2: Document the exact Coolify Scheduled Task setup in README and correct the stale enrichment-secret references</name>
  <files>README.md</files>
  <action>
Expand the README `## Deploy (Docker / Coolify)` section (and adjust the `## Configure` section where it names the enrichment secret) so the daily scheduling setup is precise and copy-pastable, and so the enrichment env matches what the app actually uses today (GLM via OPENAI_API_KEY, per quick tasks 260817-09l / 260817-jii — the deployed enrichment_provider is `openai` pointing at the GLM endpoint).

In the Deploy section, document the Coolify Scheduled Task explicitly:
- The command it runs is `python -m techtrend.daily` (the single chained collect→score→enrich entrypoint; fail-fast on the first failing stage).
- A daily cron schedule, defaulting to `0 6 * * *` (06:00 UTC), and note the owner may adjust the time.
- The required deploy environment: the PostgreSQL connection vars (`PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`), `OPENAI_API_KEY` for the GLM enrichment provider, and `GITHUB_TOKEN` for collection/backfill. Mention `TECHTREND_DISABLE_LLM` (truthy `1`/`true`/`yes`/`on`) as the enrichment opt-out that lets the scheduled run collect+score without any LLM key.
- State that the scheduled task is independent of serving: the uvicorn dashboard never triggers a pipeline run on request, and a scheduled run never depends on the dashboard being open (D-17). Reference that this matches the Dockerfile's separate CMD (serve) vs. scheduled-task (pipeline) split.
- Add a short note that missed-run handling is cron-only by design — if the container is down at fire time that day is skipped, and the skip is made visible by the dashboard's existing "last successful run" / "Data may be out of date" staleness banner (no catch-up code). This is the hosted replacement for the superseded Windows wake/run-if-missed behavior.

Correct the stale enrichment-secret references where they touch the deploy/enrichment env:
- Deploy section step that currently says to set `GITHUB_TOKEN` and `ANTHROPIC_API_KEY` in the deploy environment → make the required enrichment secret `OPENAI_API_KEY` (the deployed GLM provider), keeping `GITHUB_TOKEN`.
- Configure section bullet that currently presents `ANTHROPIC_API_KEY` as the required default enrichment secret → update it to reflect that the deployed default enrichment provider is GLM via `OPENAI_API_KEY`, with Anthropic remaining a documented alternative provider (do not delete the honest mention that neither key is required when `TECHTREND_DISABLE_LLM` is set).

Do NOT introduce any Windows Task Scheduler / `schtasks` instructions, APScheduler setup, or catch-up/backfill documentation.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; t=pathlib.Path('README.md').read_text(encoding='utf-8'); dep=t[t.index('## Deploy'):]; assert 'python -m techtrend.daily' in dep; assert re.search(r'0 6 \* \* \*|cron', dep); assert 'OPENAI_API_KEY' in dep and 'GITHUB_TOKEN' in dep; assert 'TECHTREND_DISABLE_LLM' in t; assert re.search(r'stale|out of date|last successful run', dep, re.I); assert 'schtasks' not in t and 'Task Scheduler' not in t; print('OK')"</automated>
  </verify>
  <done>The README Deploy section documents the Coolify Scheduled Task with the exact `python -m techtrend.daily` command, a daily cron schedule, the full required env (PG* + OPENAI_API_KEY + GITHUB_TOKEN, with TECHTREND_DISABLE_LLM opt-out), serving/scheduling independence (D-17), and cron-only missed-run visibility via the staleness banner; the deploy and configure enrichment-secret references name OPENAI_API_KEY (GLM) as the required secret; no Windows Task Scheduler instructions remain.</done>
</task>

<task type="auto">
  <name>Task 3: Network-free verification that SC1 & SC3 are already met by existing code + config, and SC2 is documented N/A</name>
  <files>.planning/phases/04-autonomous-daily-scheduling/04-01-SUMMARY.md</files>
  <action>
Prove — without any network call and without waiting for a live daily run — that the phase's Success Criteria are satisfied by existing code plus the documentation added in Tasks 1–2. Run the checks below and record their results in the SUMMARY (do NOT execute `techtrend.daily.main()` itself, which would attempt live collection requiring a token/network; assert the entrypoint's structure instead).

SC1 (full collect→score→enrich runs automatically once per day, no manual action): assert the chained entrypoint exists and chains the three stages in order. Import `techtrend.daily` and confirm its `STAGES` list resolves, in order, to `collect`, `score`, `enrich`, and that `techtrend/daily.py` has a `__main__` block (so `python -m techtrend.daily` is the runnable command the Coolify Scheduled Task invokes, as documented in Task 2). The "once per day, automatically" property comes from the documented Coolify cron schedule (Task 2), not from application code — record that explicitly.

SC3 (the "last successful run" indicator reflects a same-day refresh; a missed run is immediately visible): assert the existing staleness banner code in `techtrend/server/health.py` is present and unmodified — it already produces the "Last successful run: …" normal-tier message and the "Data may be out of date — last successful run was …" stale-tier message driven by `staleness_hours`. Confirm this by locating those message strings and the `staleness_hours` threshold in health.py. Record that no change to health.py was made or needed.

SC2 (original Windows wake/sleep/run-if-missed): assert it is documented as superseded/N/A on the hosted always-on server (verified via the Task 1 ROADMAP/REQUIREMENTS supersession notes and the Task 2 README missed-run note), with the hosted equivalent being the staleness banner. No implementation is expected or wanted.

Write the SUMMARY capturing: each criterion, the exact check run, its pass/fail result, and the explicit statement that SC1/SC3 are met by existing code + documented Coolify config while SC2 is superseded (not implemented). Note that live end-to-end confirmation of an actual 24h scheduled run is a deploy-time UAT item (needs the Coolify environment and a real token), deferred here by design — the plan verifies the mechanism, not a live day.
  </action>
  <verify>
    <automated>python -c "import importlib; d=importlib.import_module('techtrend.daily'); assert [s[0] for s in d.STAGES]==['collect','score','enrich'], d.STAGES; import pathlib; assert '__main__' in pathlib.Path('techtrend/daily.py').read_text(encoding='utf-8'); h=pathlib.Path('techtrend/server/health.py').read_text(encoding='utf-8'); assert 'Last successful run' in h; assert 'Data may be out of date' in h; assert 'staleness_hours' in h; print('OK')"</automated>
  </verify>
  <done>Automated checks confirm `techtrend.daily.STAGES` chains collect→score→enrich with a runnable `__main__` block (SC1 mechanism), and `techtrend/server/health.py` still renders both the "Last successful run" and "Data may be out of date" staleness messages driven by `staleness_hours` (SC3, unchanged); the SUMMARY records SC1/SC3 as met by existing code + documented Coolify config and SC2 as superseded/N/A, with the live 24h run noted as a deferred deploy-time UAT item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify deploy environment → app container | Deploy-time secrets (GITHUB_TOKEN, OPENAI_API_KEY, PGPASSWORD) are injected as env vars; this phase documents that injection but adds no code that reads new secrets. |
| Documentation → operator | README instructs the operator how to configure the scheduled task and its secrets; wrong guidance could lead to insecure secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | README deploy/scheduling docs | low | mitigate | Document secrets as deploy-environment env vars only (never inline in a committed command or config); reuse the existing `.env`/deploy-env pattern the README already establishes. No secret values are added to any repo file. |
| T-04-02 | Tampering | Rescoped ROADMAP/REQUIREMENTS text | low | accept | Docs-only edits to planning artifacts under version control; the git commit provides the audit trail and honest supersession notes preserve decision history. No runtime attack surface. |

No package-manager installs occur in this phase (documentation + verification only), so no package-legitimacy checkpoint (T-04-SC) is required. No new code path, endpoint, or dependency is introduced.
</threat_model>

<verification>
- Task 1 automated check confirms the ROADMAP/REQUIREMENTS rescope (Coolify present, supersession notes present, SCHED-01/02 intact).
- Task 2 automated check confirms the README Deploy section documents the exact command, cron schedule, required env, opt-out, staleness-based missed-run note, and carries no Windows Task Scheduler instructions.
- Task 3 automated check confirms the `techtrend.daily` entrypoint chains the three stages and that health.py's staleness banner strings are present and unmodified.
- All checks are network-free and complete in seconds; none runs a live pipeline or waits for a scheduled fire.
</verification>

<success_criteria>
- SCHED-01 closed: the daily collect→score→enrich run is configured to fire automatically via a documented Coolify Scheduled Task invoking the existing `python -m techtrend.daily` entrypoint.
- SCHED-02 rescoped: the Windows wake/sleep/run-if-missed criterion is documented as superseded/N/A on the hosted server; missed-run visibility is delegated to the existing staleness banner, with no new code.
- Planning artifacts (ROADMAP.md, REQUIREMENTS.md) and README.md are internally consistent and honest about the Windows→hosted supersession.
- The "last successful run" staleness banner is verified present and unchanged (SC3), proving a missed day is immediately visible without new work.
</success_criteria>

<output>
Create `.planning/phases/04-autonomous-daily-scheduling/04-01-SUMMARY.md` when done.
</output>
