---
phase: 01-foundation-first-signal-github-scored-and-visible
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - techtrend/collectors/__init__.py
  - techtrend/collectors/base.py
  - techtrend/collectors/github.py
  - techtrend/collectors/registry.py
  - techtrend/collectors/http.py
  - techtrend/pipeline/__init__.py
  - techtrend/pipeline/identity.py
  - techtrend/pipeline/snapshot.py
  - techtrend/pipeline/docs_link.py
  - techtrend/pipeline/orchestrator.py
  - techtrend/ingest.py
  - tests/test_collect_github.py
  - tests/test_idempotency.py
  - tests/test_docs_link.py
  - tests/test_health.py
autonomous: true
requirements: [COLL-01, COLL-06, COLL-07, COLL-08, COLL-09, DATA-01, DATA-02, DATA-05, DASH-05, HEALTH-01]

user_setup:
  - service: github
    why: "Authenticated collection — 5,000 req/hr vs 60 req/hr unauthenticated, and 304 responses only skip the rate limit when authenticated (COLL-07, COLL-08)"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub -> Settings -> Developer settings -> Personal access tokens. A fine-grained token with public-repo read access is sufficient; no write scopes are needed."

must_haves:
  truths:
    # --- Goal-backward truths ---
    - "Running `python -m techtrend.ingest` against live GitHub writes one entities row and one stars snapshot per admitted repo, and exits 0."
    - "Adding a new source requires creating a module under techtrend/collectors/ and adding one line to registry.py — the orchestrator contains no branch on source name (COLL-06)."
    - "A repo is admitted to the tracked set if it is in the seed list, OR carries a topic from the topics allowlist, OR matches the keyword list on name/description, OR is force-included; and is never admitted if force-excluded (D-01, D-03, D-04)."
    - "Every ingest run writes one run_manifest row per registered collector recording status, item_count, started_at, and finished_at (HEALTH-01)."
    - "A collector raising an exception records status 'failed' with error_detail and does not abort the run for other collectors."
    - "Each entity carries a resolved docs_url and a docs_url_kind of exactly one of 'homepage', 'readme', or 'repo' (DASH-05, D-15)."
    # --- Edge coverage: DATA-01 (adjacency, empty, ordering) ---
    - "Two collected items with identical (source, source_native_id) resolve to exactly one entities row — they merge, never collide into a duplicate (DATA-01 adjacency)."
    - "Resolving an empty collected-item list creates zero entities, writes a run_manifest row, and raises no error (DATA-01 empty)."
    - "Entity resolution is order-independent: resolving the same item set in reversed order produces an identical entities table, with ties on identical full_name ordered by entities.id ascending (DATA-01 ordering)."
    # --- Edge coverage: COLL-07 (boundary, precision) ---
    - "When a GitHub response carries x-ratelimit-remaining of 0, the collector backs off through tenacity rather than issuing a further core-API request; at x-ratelimit-remaining of 1 it still issues the request (COLL-07 boundary)."
    - "Rate-limit accounting uses integer request counts only, and the Search API 30/min budget is tracked separately from the 5000/hr core budget — no shared or fractional quota arithmetic (COLL-07 precision)."
    # --- Edge coverage: COLL-08 (boundary, precision, concurrency) ---
    - "A second identical repo-metadata request within the cache window yields HTTP 304 and does not decrement x-ratelimit-remaining (COLL-08 boundary)."
    - statement: "Stored ETag values round-trip byte-identically through hishel's storage backend with no truncation or normalization (COLL-08 precision)."
      verification: backstop
    - statement: "A collection interrupted mid-run leaves the hishel cache readable, and the next run revalidates rather than erroring (COLL-08 concurrency)."
      verification: backstop
    # --- Edge coverage: COLL-09 (adjacency, empty, ordering) ---
    - "A repo whose full_name changed but whose GitHub numeric id is unchanged resolves to the existing entity and updates full_name — it does not create a second entity (COLL-09 adjacency)."
    - "A collected item with a null or absent source_native_id is rejected before insert and counted in run_manifest, never inserted as an entity with an empty identity key (COLL-09 empty)."
    - "Identity resolution produces the same entities table regardless of the order items arrive in within one run (COLL-09 ordering)."
    # --- Edge coverage: DASH-05 (adjacency, empty, ordering) ---
    - "When both a homepage and a matching README docs link are present, homepage wins and docs_url_kind is 'homepage' — the chain returns exactly one result, never both (DASH-05 adjacency)."
    - "When homepage is empty or null and the README yields no matching link, docs_url is the repo html_url and docs_url_kind is 'repo' (DASH-05 empty)."
    - "When multiple README links match the docs patterns, the first match in document order is selected deterministically (DASH-05 ordering)."
    # --- Edge coverage: HEALTH-01 (adjacency, empty, ordering) ---
    - "A stage that succeeds but returns zero items records status 'zero_items', distinct from both 'success' and 'failed' (HEALTH-01 adjacency)."
    - "A run in which no collector produced items still writes one run_manifest row per registered collector rather than leaving the table empty (HEALTH-01 empty)."
    - "Re-running the same stage on the same run_date replaces that (run_date, stage) row rather than appending a second one (HEALTH-01 ordering)."

  prohibitions:
    - statement: "MUST NOT present a fallback repository URL as though it were documentation — when the docs-link chain terminates at the bare repo URL, the stored docs_url_kind must record 'repo' so the dashboard can label it honestly, and no code path may relabel a 'repo' fallback as 'homepage' or 'readme'."
    - statement: "MUST NOT let a collector failure or a zero-item result be recorded as a successful run — a source that stops returning data must leave a distinguishable trace in run_manifest rather than allowing the previous day's data to appear current."

  artifacts:
    - path: "techtrend/collectors/base.py"
      provides: "Collector protocol and CollectedItem model — the COLL-06 plugin seam"
      exports: ["Collector", "CollectedItem"]
    - path: "techtrend/collectors/registry.py"
      provides: "The single file touched to add a source"
      contains: "COLLECTORS"
    - path: "techtrend/collectors/github.py"
      provides: "GitHub search discovery, repo metadata, releases, README fetch"
      exports: ["GitHubCollector"]
    - path: "techtrend/collectors/http.py"
      provides: "Shared hishel CacheClient + tenacity retry predicate"
      exports: ["build_client", "is_retryable"]
    - path: "techtrend/pipeline/identity.py"
      provides: "Match-or-create entity resolution (COLL-09)"
      exports: ["resolve_entity"]
    - path: "techtrend/pipeline/snapshot.py"
      provides: "Append-only snapshot upsert (DATA-02, DATA-05)"
      exports: ["write_snapshot"]
    - path: "techtrend/pipeline/docs_link.py"
      provides: "Deterministic docs-link fallback chain (DASH-05, D-15)"
      exports: ["resolve_docs_url", "DOCS_PATTERNS"]
    - path: "techtrend/pipeline/orchestrator.py"
      provides: "Source-agnostic run driver writing run_manifest (HEALTH-01)"
      exports: ["run_collection", "record_stage"]
  key_links:
    - from: "techtrend/pipeline/orchestrator.py"
      to: "techtrend/collectors/registry.py"
      via: "iterates COLLECTORS without branching on source_id"
      pattern: "COLLECTORS"
    - from: "techtrend/collectors/github.py"
      to: "techtrend/collectors/http.py"
      via: "build_client provides the hishel-cached, tenacity-wrapped client"
      pattern: "build_client"
    - from: "techtrend/pipeline/orchestrator.py"
      to: "techtrend/pipeline/identity.py"
      via: "resolve_entity called per normalized item"
      pattern: "resolve_entity"
---

<objective>
Replace the skeleton's fixture branch with real GitHub collection: discovery + metadata + releases over an authenticated, ETag-cached, rate-limit-respecting HTTP client, flowing through a source-agnostic identity/snapshot pipeline that records per-source health for every run.

Purpose: This is the slice that makes the dashboard show *real* repositories instead of one fixture row. It also establishes the collector plugin boundary whose correctness Phase 3's entire success criterion depends on — that boundary is cheap to get right now and expensive to retrofit.

Output: `python -m techtrend.ingest` (no flags) populates the database from live GitHub, and the dashboard from plan 01-02 renders those real repos with honest docs links.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-CONTEXT.md
@.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-RESEARCH.md
@.planning/phases/01-foundation-first-signal-github-scored-and-visible/SKELETON.md
@.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by THIS plan:

| Kind | Symbol |
|---|---|
| Module | `techtrend/collectors/{__init__,base,github,registry,http}.py`, `techtrend/pipeline/{__init__,identity,snapshot,docs_link,orchestrator}.py` |
| Protocol / Class | `Collector`, `CollectedItem`, `GitHubCollector`, `StageResult` |
| Function | `build_client()`, `is_retryable()`, `resolve_entity()`, `write_snapshot()`, `resolve_docs_url()`, `run_collection()`, `record_stage()`, `admit_repo()` |
| Constant | `COLLECTORS`, `DOCS_PATTERNS`, `GITHUB_API_ROOT` |
| Env var | `GITHUB_TOKEN` |
| Test file | `tests/test_idempotency.py`, `tests/test_docs_link.py` (plus fills of `tests/test_collect_github.py`, `tests/test_health.py`) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collector plugin seam and the GitHub collector</name>
  <read_first>
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-RESEARCH.md` § "Pattern 1: Collector Plugin Interface" (the protocol and registry shape), § "Pattern 4: GitHub Collector — HTTP, Auth, Conditional Requests" (rate-limit table, the retry predicate, the discovery query shape), and § "Assumptions Log A3" (hishel constructor kwargs are unverified — confirm against the installed version)
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-CONTEXT.md` — D-01 (seed + search), D-03 (topics allowlist OR keyword fallback), D-04 (force include/exclude)
    - `techtrend/config.py` — the seed list, topics, keywords, and override lists this collector reads
    - `tests/fixtures/github/repo_metadata.json`, `tests/fixtures/github/search_repositories.json`, `tests/fixtures/github/readme.md` — the recorded responses all tests replay
    - `tests/conftest.py` — the `github_fixture` factory
  </read_first>
  <files>techtrend/collectors/__init__.py, techtrend/collectors/base.py, techtrend/collectors/http.py, techtrend/collectors/github.py, techtrend/collectors/registry.py, tests/test_collect_github.py</files>
  <behavior>
    - Test: `admit_repo` returns True for a seed-list repo, True for a repo carrying an allowlisted topic, True for an untagged repo whose description matches a keyword, True for a force-included repo, and False for a force-excluded repo even when it also matches a topic.
    - Test: `is_retryable` returns True for a 500 and for a 403 carrying `x-ratelimit-remaining: 0`, and False for a 403 without that header and for a 404 (COLL-07 boundary; permission-denied is permanent, not transient).
    - Test: a repo-metadata request replayed twice against a mock transport issues a conditional request the second time and the collector treats a 304 as a cache hit rather than an error (COLL-08 boundary).
    - Test: `GitHubCollector.normalize` maps the recorded metadata fixture onto a `CollectedItem` whose `source_native_id` is the numeric `id` as a string, never `full_name`.
    - Test: `COLLECTORS` contains exactly one entry and every element satisfies the `Collector` protocol.
  </behavior>
  <action>
    Create `techtrend/collectors/base.py` defining a `Collector` `typing.Protocol` with a `source_id: str` attribute and two methods — `fetch(self, since: date) -> list[dict]` and `normalize(self, raw: dict) -> CollectedItem` — plus a pydantic v2 `CollectedItem` model with fields `source`, `source_native_id`, `full_name`, `url`, `homepage`, `description`, `metrics` (a dict of metric name to integer value), `topics`, and `discovery_method`. `CollectedItem` is the contract every later source implements against; validate it strictly so a malformed GitHub response fails at the boundary rather than reaching the database.

    Create `techtrend/collectors/http.py` exposing `build_client()` returning a `hishel.CacheClient` wrapping `httpx`, configured with a persistent storage backend under `.hishel/`, and default headers carrying `Authorization: Bearer {GITHUB_TOKEN}` (read from the environment via `python-dotenv`), `Accept: application/vnd.github+json`, and a descriptive `User-Agent`. Confirm the exact `hishel` constructor keyword arguments against the installed version before relying on them — RESEARCH.md Assumption A3 flags these as unverified. Also expose `is_retryable(exc) -> bool` implementing the predicate from RESEARCH.md Pattern 4: retry on 5xx, retry on 403 when `x-ratelimit-remaining` is `0`, and do NOT retry a 403 without that header or any 404 — those are permanent and belong to the caller. If `GITHUB_TOKEN` is absent, raise a clear startup error naming `.env.example`; do not silently fall back to the 60 req/hr unauthenticated budget.

    Create `techtrend/collectors/github.py` defining `GitHubCollector` with `source_id = "github"`. `fetch` performs three things. First, discovery: one `GET /search/repositories` pass over the topics allowlist and a second pass using `in:name,description` over the keyword list, both sorted by stars descending at 100 per page. Track the Search API budget separately from the core budget — Search is 30/min authenticated while core is 5,000/hr, and conflating them is how a run exhausts one while thinking it has headroom. Second, union the search hits with the config seed list and the force-include list, then subtract the force-exclude list, applying `admit_repo` per D-03. Third, for each admitted repo, `GET /repos/{owner}/{repo}` for metadata, `GET /repos/{owner}/{repo}/releases?per_page=1` for the latest release, and `GET /repos/{owner}/{repo}/readme` with the raw Accept variant for docs-link extraction. Decorate the per-request helpers with `tenacity.retry` using `stop_after_attempt(5)`, `wait_exponential(multiplier=2, min=2, max=60)`, and `retry_if_exception(is_retryable)`. Never hand-roll a sleep-retry loop — `.claude/CLAUDE.md` forbids it.

    `normalize` maps a raw metadata dict onto a `CollectedItem`. `source_native_id` is `str(raw["id"])`, the numeric repo id — this is the value that survives a rename and is what COLL-09's adjacency case depends on. `metrics` carries `stars` from `stargazers_count` and `releases` from the release count, representing releases as a second metric in the same snapshots table rather than a separate concern (this settles the CONTEXT.md discretion item on release representation).

    Create `techtrend/collectors/registry.py` containing exactly a `COLLECTORS` list holding one `GitHubCollector()` instance, with a module docstring stating that this file is the only one that changes when a source is added.

    Fill `tests/test_collect_github.py` with the five behaviors above, driving HTTP through an `httpx.MockTransport` seeded from the recorded fixtures. No test makes a live network call.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_collect_github.py -q` exits 0
    - `python -c "from techtrend.collectors.registry import COLLECTORS; print(len(COLLECTORS), COLLECTORS[0].source_id)"` prints `1 github`
    - `techtrend/collectors/http.py` contains the literals `tenacity`, `hishel`, and `x-ratelimit-remaining`
    - `techtrend/collectors/github.py` contains the literal `search/repositories`
    - `techtrend/collectors/github.py` contains the literal `in:name,description`
    - `grep -c 'time.sleep' techtrend/collectors/github.py techtrend/collectors/http.py` reports `0` for both — retry is delegated to tenacity
    - `grep -c 'GITHUB_TOKEN' techtrend/collectors/github.py` returns `0` — the token is read only in `http.py`
    - `python -m pytest tests/test_collect_github.py::test_is_retryable_403_permission_denied_not_retried -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_collect_github.py -q</automated>
  </verify>
  <done>A registered GitHub collector discovers, filters, and fetches real repositories over an authenticated, cached, backoff-protected client, and the registry is the single extension point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity resolution, snapshot upsert, and the source-agnostic orchestrator</name>
  <read_first>
    - `techtrend/db/schema.sql` — the constraints that make this idempotent
    - `techtrend/collectors/base.py` — the `CollectedItem` contract created in Task 1
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-RESEARCH.md` § "Pattern 1" (the orchestrator loop with run_manifest recording), § "Pattern 2 → Upsert", § "Common Pitfalls → Pitfall 1: Silent collector failure"
    - `techtrend/ingest.py` — created in 01-02; its fixture branch is preserved and a live branch added
  </read_first>
  <files>techtrend/pipeline/__init__.py, techtrend/pipeline/identity.py, techtrend/pipeline/snapshot.py, techtrend/pipeline/orchestrator.py, techtrend/ingest.py, tests/test_idempotency.py, tests/test_health.py</files>
  <behavior>
    - Test: resolving two items with the same `(source, source_native_id)` yields one entities row (DATA-01 adjacency).
    - Test: resolving an item whose `full_name` changed but whose `source_native_id` is unchanged updates the existing row and leaves `COUNT(*)` at 1 (COLL-09 adjacency).
    - Test: resolving an item with a null or empty `source_native_id` raises no exception, inserts nothing, and increments a rejected count recorded in run_manifest (COLL-09 empty).
    - Test: resolving the same item list in reversed order produces an identical entities table by `(source, source_native_id, full_name)` (DATA-01/COLL-09 ordering).
    - Test: resolving an empty item list creates zero entities and still writes a run_manifest row (DATA-01 empty).
    - Test: running the full collection twice against the same fixtures and the same run_date leaves entity and snapshot counts unchanged (DATA-05).
    - Test: a collector whose `fetch` raises records `status='failed'` with a non-null `error_detail`, and a second registered collector still runs.
    - Test: a collector returning an empty list records `status='zero_items'`, distinct from both `'success'` and `'failed'` (HEALTH-01 adjacency).
    - Test: with no collector producing items, one run_manifest row exists per registered collector (HEALTH-01 empty).
    - Test: recording the same `(run_date, stage)` twice leaves one row with the later values (HEALTH-01 ordering).
  </behavior>
  <action>
    Create `techtrend/pipeline/identity.py` exposing `resolve_entity(conn, item: CollectedItem, now: datetime) -> int | None` implementing COLL-09 match-or-create. It runs the entities upsert from RESEARCH.md Pattern 2 keyed on `ON CONFLICT(source, source_native_id)`, updating `full_name`, `url`, `homepage`, and `last_seen_at` from `excluded`, and returns the entity id. Guard the identity key first: when `source_native_id` is None or empty after stripping, log a WARNING, return None, and let the caller count it as rejected — an entity with an empty identity key would silently collide with every other such entity. On insert, set `admitted_at` to now and `discovery_method` from the item. Never update `admitted_at` on conflict; first-admission time is history.

    Create `techtrend/pipeline/snapshot.py` exposing `write_snapshot(conn, entity_id: int, collected_at: str, metric_name: str, metric_value: int, source_kind: str = "observed") -> None` running the snapshots upsert keyed on `ON CONFLICT(entity_id, collected_at, metric_name)` with `metric_value = excluded.metric_value`. The `source_kind` parameter exists so plan 01-05 can write backfilled points through the same function with `'backfill'` (D-07) rather than a parallel write path.

    Create `techtrend/pipeline/orchestrator.py` exposing `record_stage(conn, run_date, stage, status, item_count=None, error_detail=None, started_at=None, finished_at=None)` — an upsert on `(run_date, stage)` so a re-run replaces rather than appends — and `run_collection(conn, run_date, collectors) -> list[StageResult]`. `run_collection` iterates the passed collector list without branching on `source_id`; a `grep` for the literal `github` in this file must find nothing. Per collector: record the stage as started, call `fetch`, normalize each raw item, `resolve_entity`, then `write_snapshot` for every entry in the item's `metrics` dict. On success with a non-zero item count record `'success'`; on success with zero items record `'zero_items'` — this is the Pitfall 1 mitigation and it must be distinguishable from `'success'` because a source that quietly dies returns 200 with nothing, not an exception. On any exception record `'failed'` with `error_detail` set to the exception string, then continue to the next collector so one dead source cannot take the run down. Truncate `error_detail` to 500 characters before storing.

    Modify `techtrend/ingest.py` so the default (no-flag) path calls `run_collection` with `COLLECTORS` from the registry, while `--fixture` retains the offline path from 01-02. Commit the transaction once per collector stage so a mid-run crash leaves completed stages durable and re-runnable.

    Fill `tests/test_idempotency.py` and `tests/test_health.py` with the ten behaviors above.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_idempotency.py tests/test_health.py -q` exits 0 with 10 or more passing tests
    - `grep -c 'github' techtrend/pipeline/orchestrator.py` returns `0` — the orchestrator never branches on source
    - `techtrend/pipeline/orchestrator.py` contains the literal `zero_items`
    - `techtrend/pipeline/identity.py` contains the literal `ON CONFLICT(source, source_native_id)`
    - `techtrend/pipeline/snapshot.py` contains the literal `ON CONFLICT(entity_id, collected_at, metric_name)`
    - `techtrend/pipeline/snapshot.py` contains the literal `source_kind`
    - `python -m pytest tests/test_idempotency.py::test_rerun_no_duplicate_entities -q` exits 0
    - `python -m pytest tests/test_health.py::test_zero_items_flagged_not_silent -q` exits 0
    - `python -m pytest -q` exits 0 (full suite, including 01-01 and 01-02 tests)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_idempotency.py tests/test_health.py -q</automated>
  </verify>
  <done>Collected items resolve to stable entities and append-only snapshots, a partially-failed run is safe to re-run, and every run leaves a per-source health record that distinguishes success from silence from failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic docs-link resolution with honest labeling</name>
  <read_first>
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-CONTEXT.md` — D-15 (the fallback chain and the honest-labeling requirement) and § "Specific Ideas" (honest labeling over silent wrongness)
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-RESEARCH.md` § "Pattern 6: Docs-Link Resolution Chain (D-15)" — the pattern list and the README extraction approach
    - `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-UI-SPEC.md` § "Copywriting Contract" — the label strings the stored `docs_url_kind` drives
    - `tests/fixtures/github/readme.md` — the recorded README the chain is tested against
    - `techtrend/pipeline/identity.py` — where the resolved values are persisted onto the entity
  </read_first>
  <files>techtrend/pipeline/docs_link.py, techtrend/pipeline/identity.py, tests/test_docs_link.py</files>
  <behavior>
    - Test: metadata with a non-empty `homepage` returns that URL with kind `homepage`, even when the README also contains a matching docs link (DASH-05 adjacency — exactly one result, homepage wins).
    - Test: metadata with an empty `homepage` and a README containing a link whose URL or link text matches a docs pattern returns that link with kind `readme`.
    - Test: metadata with an empty `homepage` and a README with no matching link returns `html_url` with kind `repo` (DASH-05 empty).
    - Test: a README containing several matching links returns the first in document order, and the result is identical across repeated calls (DASH-05 ordering).
    - Test: the returned kind is always exactly one of the three literal values, and a `repo` result is never relabeled.
  </behavior>
  <action>
    Create `techtrend/pipeline/docs_link.py` exposing `DOCS_PATTERNS` — a tuple containing the substrings `docs.`, `/docs`, `documentation`, `getting-started`, `getting_started`, and `quickstart` — and `resolve_docs_url(repo_meta: dict, readme_text: str | None) -> tuple[str, str]` returning `(url, kind)` where kind is one of `homepage`, `readme`, or `repo`.

    The chain is strictly ordered per D-15. First, if `repo_meta` has a non-empty, whitespace-stripped `homepage`, return it with kind `homepage` and stop — do not also consult the README. Second, extract links from `readme_text` with a regex covering markdown link syntax and bare `http(s)` URLs, preserving document order, and return the first link whose URL or link text case-insensitively contains any `DOCS_PATTERNS` entry, with kind `readme`. Do not pull in a markdown parser for link extraction alone. Third, return `repo_meta["html_url"]` with kind `repo`.

    The third branch is the load-bearing one: it is a repository URL, not documentation, and the stored kind must say so. The dashboard labels a `repo` result differently from a `docs` result precisely so the user is never sent somewhere wrong under a confident label. No code path may rewrite a `repo` kind to `homepage` or `readme` after the fact.

    Modify `techtrend/pipeline/identity.py` so the entities upsert also writes `docs_url` and `docs_url_kind` from `resolve_docs_url`, updating both on conflict so a repo that later gains a homepage gets an improved link on the next run.

    Fill `tests/test_docs_link.py` with the five behaviors above, driving the README cases from the recorded fixture.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_docs_link.py -q` exits 0 with 5 or more passing tests
    - `python -c "from techtrend.pipeline.docs_link import resolve_docs_url; print(resolve_docs_url({'homepage':'https://x.dev','html_url':'https://github.com/a/b'}, '[docs](https://docs.y.dev)'))"` prints a tuple whose second element is `homepage`
    - `python -c "from techtrend.pipeline.docs_link import resolve_docs_url; print(resolve_docs_url({'homepage':'','html_url':'https://github.com/a/b'}, 'no links here'))"` prints a tuple equal to `('https://github.com/a/b', 'repo')`
    - `python -c "from techtrend.pipeline.docs_link import DOCS_PATTERNS; print(len(DOCS_PATTERNS) >= 6)"` prints `True`
    - `techtrend/pipeline/identity.py` contains the literal `docs_url_kind`
    - `python -m pytest -q` exits 0 (full suite)
    - `ruff check .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_docs_link.py -q && python -m pytest -q</automated>
  </verify>
  <done>Every entity carries a resolved documentation link whose provenance is recorded, and a fallback to the bare repository URL is stored as such rather than passed off as documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → process | Every field of every repo record is attacker-influenceable — any GitHub user can create a repo with a crafted name, description, homepage, or README |
| environment → process | `GITHUB_TOKEN` enters from `.env` |
| process → GitHub API | Outbound authenticated requests consume a finite, shared rate-limit budget |
| process → log file | Response metadata is written to `logs/techtrend.log` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | GitHub response fields reaching `entities`/`snapshots` | high | mitigate | Every raw record is validated through the pydantic v2 `CollectedItem` model at the collector boundary; a malformed or unexpected shape fails before it reaches SQL. All SQL uses bound parameters, never string interpolation |
| T-01-12 | Information Disclosure | `GITHUB_TOKEN` reaching `logs/techtrend.log` | high | mitigate | The token is read in `http.py` only and injected once into default headers; log statements record status codes, URLs, and item counts — never headers. Enforced by an acceptance-criteria grep confining `GITHUB_TOKEN` to `http.py` |
| T-01-13 | Denial of Service | Rate-limit exhaustion against GitHub (self-inflicted denial of wallet) | high | mitigate | hishel conditional requests mean unchanged resources return 304 and cost no quota; tenacity backs off on 403-with-remaining-0 and 5xx; the Search budget is tracked separately from core so one cannot silently drain the other; the per-repo request cap lands in plan 01-05 |
| T-01-14 | Denial of Service | A single failing collector aborting the whole run | medium | mitigate | `run_collection` catches per collector, records `'failed'`, and continues — one dead source cannot take the run down |
| T-01-15 | Spoofing | A crafted repo URL in `homepage` or a README link stored as the docs target | medium | mitigate | The URL is stored as data and rendered as an `href` under Jinja2 autoescaping; it is never fetched, executed, or followed by the application. The honest `docs_url_kind` label prevents implying the target was verified |
| T-01-16 | Repudiation | No record of which source failed on which day | medium | mitigate | `run_manifest` records `(run_date, stage, status, item_count, error_detail, started_at, finished_at)` for every collector on every run |
| T-01-17 | Tampering | README text treated as trusted input | low | accept | Phase 1 only regex-extracts a URL string from README text; it does not render, execute, or send it to an LLM. This becomes a prompt-injection concern in Phase 2 and is scoped there |

**Canon-referral breadcrumbs:** SQL injection and secret-management hygiene are canon OWASP/ASVS — the parameterized-query and token-confinement controls above are recorded as mitigations, but the general classes are covered by `/gsd-secure-phase`; not minted as bespoke prohibitions here.
</threat_model>

<planner_assumptions>
Four probe-surfaced edges for this plan's requirements returned `unclassified` from the deterministic edge probe and are therefore carried forward as explicit, unresolved planner assumptions rather than auto-resolved into acceptance criteria:

1. **DATA-02 (unclassified)** — the probe could not classify an edge shape for "append-only timestamped snapshots." Assumption: day-granularity `collected_at` is the correct resolution, so at most one observation per entity per metric per day. Sub-daily re-runs overwrite rather than append. Flag if intraday velocity ever matters.
2. **DATA-05 (unclassified)** — the probe could not classify an edge shape for partial-run re-runnability. Assumption: idempotency is provided entirely by the three schema uniqueness constraints, with no separate resume-cursor state. A re-run redoes completed stages rather than skipping them; this is cheap here and avoids a second source of truth about run progress.
3. **COLL-01 (unclassified)** — the probe could not classify an edge shape for "collect star counts and release events." Assumption: releases are represented as a second metric row (`metric_name='releases'`) in the same `snapshots` table rather than as their own table, settling the CONTEXT.md discretion item. Revisit if release *metadata* (tag, notes, date) is later needed rather than just a count.
4. **COLL-06 (unclassified)** — the probe could not classify an edge shape for the plugin-boundary requirement. Assumption: the boundary is verified structurally (the orchestrator contains no source-name branch, enforced by grep) rather than behaviorally, since a second source does not exist until Phase 3. Phase 3 is the real test.
</planner_assumptions>

<verification>
- `python -m pytest -q` exits 0 across the whole suite
- `python -m techtrend.ingest` with a real `GITHUB_TOKEN` populates entities and snapshots and writes a `success` run_manifest row
- An immediate second run shows 304 responses in the log and leaves row counts unchanged
- `grep github techtrend/pipeline/orchestrator.py` finds nothing
- `ruff check .` exits 0
</verification>

<success_criteria>
Real AI-coding GitHub repositories flow from live discovery through an authenticated, cached, backoff-protected client into stable entities and append-only snapshots; every run leaves a per-source health record; every entity carries an honestly-labeled documentation link; and adding a second source would touch only a new collector module plus one registry line.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-first-signal-github-scored-and-visible/01-03-SUMMARY.md` when done
</output>
