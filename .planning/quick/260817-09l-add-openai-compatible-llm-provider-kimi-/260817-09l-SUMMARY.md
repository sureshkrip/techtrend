---
phase: quick-260817-09l
plan: "01"
subsystem: enrichment
tags: [openai, kimi, moonshot, llm, config, structured-output]

requires:
  - phase: 02-05
    provides: techtrend.pipeline.llm's EnrichmentResult/Confidence/build_section_result_model contract and enrich.py's run_enrichment orchestration loop
provides:
  - Config-selectable OpenAI-compatible LLM provider (enrichment_provider tunable) for the enrichment summarize+classify call
  - techtrend/pipeline/llm_openai.py mirroring llm.py's key isolation and None-return anti-fabrication contract
  - enrich.py dispatch branch routing to Kimi/Moonshot when enrichment_provider="openai"
affects: [enrichment, config]

tech-stack:
  added: ["openai==3.1.0"]
  patterns: ["Provider dispatch via config tunable with lazy import of the non-default provider module", "Shared EnrichmentResult/Confidence/build_section_result_model contract reused across providers, not duplicated"]

key-files:
  created:
    - techtrend/pipeline/llm_openai.py
    - tests/test_llm_openai.py
  modified:
    - pyproject.toml
    - uv.lock
    - techtrend/config.py
    - techtrend/pipeline/enrich.py
    - README.md

key-decisions:
  - "Pinned openai==3.1.0 as an exact version (matching the codebase's existing exact-pin convention) after uv add openai resolved it as >=3.1.0 by default"
  - "enrich.py's llm_call_fn branch dispatches on tunables.enrichment_provider, lazily importing llm_openai only inside the openai branch so an anthropic-only run never imports the openai package"
  - "enrich_item_openai reuses EnrichmentResult/Confidence/build_section_result_model from llm.py rather than redefining them, keeping the section-enum enforcement and output schema identical across providers"

patterns-established:
  - "Multi-provider LLM dispatch: a single config tunable (enrichment_provider) selects the branch; each provider module owns its own API-key env var in isolation (grep-confined), mirroring the existing GITHUB_TOKEN/ANTHROPIC_API_KEY isolation pattern"

requirements-completed: []

coverage:
  - id: D1
    description: "enrichment_provider config toggle: default 'anthropic' unchanged, 'openai' routes to Kimi/Moonshot"
    verification:
      - kind: unit
        ref: "verify command: Tunables().enrichment_provider == 'anthropic' and Tunables().enrichment_base_url == 'https://api.moonshot.ai/v1' — pass"
        status: pass
    human_judgment: false
  - id: D2
    description: "llm_openai.enrich_item_openai returns None on empty content, refusal (content_filter), JSON parse failure, and Pydantic validation failure (no fabrication)"
    verification:
      - kind: unit
        ref: "tests/test_llm_openai.py#test_enrich_item_openai_returns_none_on_empty_content"
        status: pass
      - kind: unit
        ref: "tests/test_llm_openai.py#test_enrich_item_openai_returns_none_on_non_json_content"
        status: pass
      - kind: unit
        ref: "tests/test_llm_openai.py#test_enrich_item_openai_returns_none_on_refusal"
        status: pass
      - kind: unit
        ref: "tests/test_llm_openai.py#test_enrich_item_openai_returns_none_on_invalid_section"
        status: pass
    human_judgment: false
  - id: D3
    description: "llm_openai.enrich_item_openai returns a valid EnrichmentResult on success"
    verification:
      - kind: unit
        ref: "tests/test_llm_openai.py#test_enrich_item_openai_returns_two_line_summary"
        status: pass
    human_judgment: false
  - id: D4
    description: "enrich.py dispatches on enrichment_provider without changing the default anthropic path or run_enrichment's public seams"
    verification:
      - kind: unit
        ref: "tests/test_enrich.py (full suite, unchanged, still passing)"
        status: pass
    human_judgment: false
  - id: D5
    description: "OPENAI_API_KEY is read only inside techtrend/pipeline/llm_openai.py (grep-confined key isolation)"
    verification:
      - kind: other
        ref: "grep -rn \"OPENAI_API_KEY\" techtrend/ --include=\"*.py\" — only llm_openai.py matches"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-17
status: complete
---

# Quick Task 260817-09l: OpenAI-compatible LLM provider (Kimi/Moonshot) Summary

**Config-selectable OpenAI-compatible enrichment provider (enrichment_provider tunable) routing to Kimi/Moonshot's chat-completions API, reusing llm.py's EnrichmentResult/Confidence/section-enum contract with matching anti-fabrication guarantees, while Anthropic Haiku 4.5 remains the untouched default.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-17T03:55:00Z
- **Completed:** 2026-08-17T04:19:29Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Added `openai==3.1.0` dependency (exact pin, resolved and locked via `uv add openai` + `uv lock`)
- Added two new `Tunables` fields: `enrichment_provider` (default `"anthropic"`) and `enrichment_base_url` (default Moonshot's `https://api.moonshot.ai/v1`)
- Created `techtrend/pipeline/llm_openai.py`: `MissingOpenAIKeyError`, `build_openai_client(*, base_url, client=None)`, and `enrich_item_openai(...)` mirroring `llm.py`'s contract and key-isolation, importing `EnrichmentResult`/`Confidence`/`build_section_result_model` from `llm.py` rather than duplicating them
- Wired a provider dispatch branch into `enrich.py`'s existing `if llm_call_fn is None:` block — `openai` branch lazily imports `llm_openai` and builds a Kimi/Moonshot client; `anthropic` branch (default) is verbatim/untouched
- Added `tests/test_llm_openai.py` with a fake OpenAI-shaped client covering success, empty content, non-JSON content, refusal (`finish_reason == "content_filter"`), and invalid-section-id validation failure — all return `None` except the success case
- Documented the Kimi/Moonshot switch (`enrichment_provider`, `enrichment_model`, `enrichment_base_url`, `OPENAI_API_KEY`) in README.md's Configure section

## Task Commits

Each task was committed atomically:

1. **Task 1: Add openai dep, config knobs, and the llm_openai module** - `2ecaf4c` (feat)
2. **Task 2: Dispatch on enrichment_provider in enrich.py + tests + README** - `ef50e98` (feat)

**Plan metadata:** (pending — orchestrator handles the docs commit)

## Files Created/Modified
- `techtrend/pipeline/llm_openai.py` - New module: OpenAI-compatible client builder + `enrich_item_openai`, key isolation, no-fabrication contract
- `tests/test_llm_openai.py` - New tests covering success + all None-return paths with a fake client (no live calls)
- `pyproject.toml` - Added `openai==3.1.0` exact pin
- `uv.lock` - Regenerated lockfile including openai and its transitive deps (httpx2, httpcore2, tqdm, truststore)
- `techtrend/config.py` - Added `Tunables.enrichment_provider` and `Tunables.enrichment_base_url`
- `techtrend/pipeline/enrich.py` - `run_enrichment`'s `llm_call_fn` construction now branches on `tunables.enrichment_provider`
- `README.md` - Documented the Kimi/Moonshot provider switch under Configure

## Decisions Made
- `uv add openai` resolved a caret-range pin (`openai>=3.1.0`); hand-edited to the exact `openai==3.1.0` to match the codebase's existing exact-pin convention for all other dependencies, then ran `uv lock` to regenerate the lockfile consistently.
- Verified `openai==3.1.0` resolved from the project's real configured index via `uv add` (not hand-substituted), and confirmed its installed package path/version directly (`.venv/Lib/site-packages/openai/__init__.py`, `openai.__version__ == "3.1.0"`) before proceeding — no package-legitimacy checkpoint was needed since the install succeeded cleanly through the trusted resolver.
- `enrich_item_openai`'s system prompt embeds the valid section-id list and confidence values explicitly (since OpenAI-compatible `response_format={"type": "json_object"}` mode has no native JSON-Schema enum enforcement the way Anthropic's `output_format=` structured-output does) — validation is still enforced server-side (in this codebase) via `build_section_result_model`'s Pydantic `StrEnum`, so an out-of-enum id from the model is caught and returns `None`, never silently coerced.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

`ruff check techtrend tests` surfaced 3 pre-existing E501 (line-too-long) errors unrelated to this task's changes: `techtrend/pipeline/enrich.py:238` (a line above/below this task's edit, confirmed pre-existing via `git show HEAD~1:...`), `tests/test_dashboard.py:126`, and `tests/test_grounding.py:75`. Per the scope-boundary rule (only auto-fix issues directly caused by this task's changes), these were left unfixed and logged to `.planning/quick/260817-09l-add-openai-compatible-llm-provider-kimi-/deferred-items.md` rather than fixed inline. `ruff check` on the files this task actually created/modified (`llm_openai.py`, the new test file, `config.py`) is clean.

## User Setup Required

None for the default path (Anthropic remains unchanged). To use Kimi/Moonshot instead: set `enrichment_provider = "openai"` and `enrichment_model` under `[tunables]` in `config/tracked.toml`, and set `OPENAI_API_KEY` in `.env` — documented in README.md's Configure section.

## Next Phase Readiness

Enrichment can now target either provider via a single config toggle with no code changes required. Phase 3 (Source Breadth) is unaffected and unblocked by this change.

---
*Quick task: 260817-09l*
*Completed: 2026-08-17*

## Self-Check: PASSED

- FOUND: techtrend/pipeline/llm_openai.py
- FOUND: tests/test_llm_openai.py
- FOUND commit: 2ecaf4c
- FOUND commit: ef50e98
