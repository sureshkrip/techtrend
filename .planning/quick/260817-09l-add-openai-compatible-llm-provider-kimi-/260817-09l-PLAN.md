---
phase: quick-260817-09l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - techtrend/config.py
  - techtrend/pipeline/llm_openai.py
  - techtrend/pipeline/enrich.py
  - tests/test_llm_openai.py
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Setting enrichment_provider = \"openai\" in config routes the summarize+classify call to an OpenAI-compatible endpoint (Moonshot/Kimi) instead of Anthropic."
    - "Default behavior is unchanged: enrichment_provider defaults to \"anthropic\" and the existing Anthropic Haiku 4.5 path is untouched."
    - "The OpenAI provider never fabricates: empty/missing content, a refusal, a JSON parse failure, or a Pydantic validation failure all return None (caller writes a fetch_failed tombstone)."
    - "OPENAI_API_KEY is read only inside techtrend/pipeline/llm_openai.py (grep-confined key isolation, mirroring llm.py)."
  artifacts:
    - techtrend/pipeline/llm_openai.py
    - tests/test_llm_openai.py
  key_links:
    - "enrich.py dispatches on config.tunables.enrichment_provider, lazily importing llm_openai only on the openai branch."
    - "llm_openai reuses EnrichmentResult / Confidence / build_section_result_model imported from techtrend.pipeline.llm (no duplication)."
---

<objective>
Add a config-selectable OpenAI-compatible LLM provider so the enrichment summarize+classify call can target Kimi (Moonshot AI) as an alternative to Anthropic, while keeping Anthropic Haiku 4.5 as the unchanged default.

Purpose: The user wants to run enrichment against Kimi/Moonshot via its OpenAI-compatible API without disturbing the existing, verified Anthropic path or its anti-fabrication guarantees.
Output: `openai` dependency added, new `llm_openai.py` module mirroring llm.py's contract and key-isolation, two new Tunables config knobs, a provider dispatch branch in enrich.py, new tests, and README documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@techtrend/pipeline/llm.py
@techtrend/pipeline/enrich.py
@techtrend/config.py
@tests/test_llm.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openai dep, config knobs, and the llm_openai module</name>
  <files>pyproject.toml, uv.lock, techtrend/config.py, techtrend/pipeline/llm_openai.py</files>
  <action>
Add the `openai` dependency and resolve its exact pin via uv: run `uv add openai`, which updates BOTH pyproject.toml `[project].dependencies` and uv.lock consistently. Do NOT hand-edit uv.lock. If `uv add` is unavailable, hand-pin an exact `openai==X.Y.Z` (recent release) in pyproject.toml `[project].dependencies` alongside the existing exact pins (e.g. `anthropic==0.122.0`) and note the fallback in the SUMMARY.

In `techtrend/config.py`, add two fields to the `Tunables` model, following the existing single-line commented-field style: `enrichment_provider: str = "anthropic"` (valid values "anthropic"|"openai" — note this in the comment) and `enrichment_base_url: str = "https://api.moonshot.ai/v1"` (the OpenAI-compatible base URL used only when the provider is "openai"). Note in comments that `enrichment_model` (already present) is reused as the model id for whichever provider is selected. Do NOT change any existing field, default, or the Config/load_config machinery.

Create `techtrend/pipeline/llm_openai.py` mirroring llm.py's security-isolation and None-return contract:
  - Module docstring stating OPENAI_API_KEY is read ONLY in this module (grep-enforced isolation, mirroring ANTHROPIC_API_KEY in llm.py) and that the summarize+classify contract, anti-fabrication, and anti-prompt-injection behavior mirror llm.py.
  - Import `openai`. Import `EnrichmentResult`, `Confidence`, and `build_section_result_model` from `techtrend.pipeline.llm` — do NOT redefine those classes here.
  - `class MissingOpenAIKeyError(RuntimeError)` mirroring `MissingAnthropicKeyError`, with a message telling the user to set OPENAI_API_KEY in .env.
  - `_get_openai_key() -> str` reading `os.environ.get("OPENAI_API_KEY")`, raising MissingOpenAIKeyError at call time when absent.
  - `build_openai_client(*, base_url: str, client=None)` returning the injected `client` if given (tests), else `openai.OpenAI(api_key=_get_openai_key(), base_url=base_url)`. base_url is a required keyword param (no hardcoded Moonshot URL in this module — the default lives in config).
  - `enrich_item_openai(client, *, model, sections, description, readme_intro) -> EnrichmentResult | None` with the SAME signature shape as llm.py's `enrich_item`. Build `section_ids` and `result_model = build_section_result_model(section_ids)`. Build a `section_defs` block identically to llm.py. Call `client.chat.completions.create(model=model, max_tokens=512, response_format={"type": "json_object"}, messages=[system, user])`. The system message MUST carry the SAME anti-fabrication + anti-prompt-injection + section-definitions instructions as llm.py's system prompt, PLUS an explicit instruction to return ONLY a JSON object with keys summary_line_1, summary_line_2, section, confidence — listing the valid section ids and the valid confidence values high|medium|low. The user message MUST use the same `<repo_description>`/`<readme_excerpt>` structure as llm.py. Read `response.choices[0].message.content`; also read `response.choices[0].finish_reason`. Return `None` on ANY of: missing/empty content, a content-policy refusal (e.g. finish_reason == "content_filter"), a `json.loads` parse failure, or a Pydantic `ValidationError` from `result_model.model_validate(...)`. On success return the validated `result_model` instance. Wrap the parse+validate in try/except returning None (never raise, never fabricate). Do NOT add any hand-rolled retry/backoff — the openai SDK retries internally (mirrors llm.py's no-tenacity decision).
  - NOTE: do not place fenced code blocks in this file's own comments; keep any negated-literal-free comment text so no acceptance-criteria grep self-invalidates.
  </action>
  <verify>
    <automated>cd C:/Users/sures/dev/repos/techtrend && uv run python -c "import techtrend.pipeline.llm_openai as m; from techtrend.config import Tunables; t=Tunables(); assert t.enrichment_provider=='anthropic'; assert t.enrichment_base_url=='https://api.moonshot.ai/v1'; assert hasattr(m,'build_openai_client') and hasattr(m,'enrich_item_openai') and hasattr(m,'MissingOpenAIKeyError'); print('ok')"</automated>
  </verify>
  <done>`openai` is in pyproject.toml (and uv.lock if uv used); Tunables has enrichment_provider (default "anthropic") and enrichment_base_url (default Moonshot); llm_openai.py imports EnrichmentResult/Confidence/build_section_result_model from llm.py (not redefined), isolates OPENAI_API_KEY, and enrich_item_openai returns None on empty/refusal/parse/validation failure.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch on enrichment_provider in enrich.py + tests + README</name>
  <files>techtrend/pipeline/enrich.py, tests/test_llm_openai.py, README.md</files>
  <action>
In `techtrend/pipeline/enrich.py`, inside the existing `if llm_call_fn is None:` block ONLY, add a branch on `config.tunables.enrichment_provider` (via the local `tunables` variable). For provider == "openai": lazily import `from techtrend.pipeline.llm_openai import build_openai_client, enrich_item_openai` inside the branch (so anthropic-only runs never import openai), build the client with `build_openai_client(base_url=tunables.enrichment_base_url)`, and set the `llm_call_fn` closure to call `enrich_item_openai(openai_client, model=enrichment_model, sections=section_dicts, **kwargs)`. For anything else (default "anthropic"), keep the EXISTING behavior verbatim — `build_llm_client()` + `enrich_item`, with the existing top-level import untouched. Reuse the existing `section_dicts` and `enrichment_model` locals for both branches. Do NOT change run_enrichment's or select_candidates' signatures, the cost-gating/cap SQL, the cache-hit logic, the tombstone logic, or the top-level `from techtrend.pipeline.llm import build_llm_client, enrich_item` import.

Create `tests/test_llm_openai.py` mirroring tests/test_llm.py's structure (imports inside each test function; no live API calls). Define a `_FakeOpenAIClient` exposing `.chat.completions.create(**kwargs)` that returns a fake response object whose `.choices[0].message.content` is a JSON string and `.choices[0].finish_reason` is set — build these with small fake classes (fake response -> fake choice -> fake message). Reuse a small SECTIONS list like test_llm.py. Cover at minimum:
  (a) success: content is a valid JSON object with summary_line_1, summary_line_2, section (a valid id), confidence "high"; finish_reason "stop" -> enrich_item_openai returns a result whose two summary lines and section match the JSON.
  (b) malformed/empty: separate assertions for content=None, content="not json at all", and a refusal (finish_reason "content_filter") -> each returns None.
  (c) invalid section id: content is valid JSON but section is "not_a_real_section" -> returns None (build_section_result_model enum validation failure).
Reuse the JSON shape inline (no fixtures dir needed). Import `enrich_item_openai` inside each test function.

In `README.md`, extend the Configure section (near lines 28-33, the credentials + Tunables block): document that to use Kimi/Moonshot instead of Anthropic, set under `[tunables]` in `config/tracked.toml`: `enrichment_provider = "openai"`, `enrichment_model` to a Moonshot model id (example `kimi-k2.5`), and optionally `enrichment_base_url` (defaults to `https://api.moonshot.ai/v1`); and set `OPENAI_API_KEY` in `.env`. Note the default provider is Anthropic Haiku 4.5 and these knobs live under `[tunables]`. Do NOT edit `.env` or `.env.example` (permission-restricted) — document in README only.
  </action>
  <verify>
    <automated>cd C:/Users/sures/dev/repos/techtrend && uv run pytest tests/test_llm.py tests/test_llm_openai.py tests/test_enrich.py -q</automated>
  </verify>
  <done>enrich.py branches on enrichment_provider (openai lazily imports llm_openai, anthropic path verbatim/untouched); tests/test_llm_openai.py passes covering success + empty/non-json/refusal->None + invalid-section->None with a fake client and no live calls; existing tests/test_llm.py still passes; README documents OPENAI_API_KEY and the Kimi switch under [tunables].</done>
</task>

</tasks>

<verification>
- Full suite green: `cd C:/Users/sures/dev/repos/techtrend && uv run pytest -q` (existing Anthropic tests unchanged and passing).
- Key isolation: `OPENAI_API_KEY` literal appears only in `techtrend/pipeline/llm_openai.py` — `grep -rn "OPENAI_API_KEY" techtrend/` returns that file only.
- Default unchanged: `Tunables().enrichment_provider == "anthropic"`; enrich.py anthropic branch identical to prior behavior.
</verification>

<success_criteria>
- Config toggle `enrichment_provider = "openai"` routes enrichment to the OpenAI-compatible endpoint; default remains Anthropic Haiku 4.5.
- enrich_item_openai returns None on empty content, refusal, JSON parse failure, and Pydantic validation failure (no fabrication).
- EnrichmentResult/Confidence/build_section_result_model reused from llm.py (not duplicated); no hand-rolled retry.
- New and existing tests pass; README documents the Kimi switch.
</success_criteria>

<output>
Create `.planning/quick/260817-09l-add-openai-compatible-llm-provider-kimi-/260817-09l-SUMMARY.md` when done.
</output>
